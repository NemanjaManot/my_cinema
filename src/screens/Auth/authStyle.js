import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    textInputStyle: {
        backgroundColor: 'transparent'
    },
    loginButton: {
        padding: 15
    },
    inputStyle: {
        marginBottom: 15
    }
});
